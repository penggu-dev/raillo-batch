services:
  mysql:
    image: 'mysql:8'
    environment:
      - 'MYSQL_DATABASE=raillo'
      - 'MYSQL_PASSWORD=secret'
      - 'MYSQL_ROOT_PASSWORD=secret'
      - 'MYSQL_USER=sa'
    ports:
      - '3306:3306'